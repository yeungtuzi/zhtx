head	1.3;
access;
symbols;
locks
	mudadm:1.3; strict;
comment	@ * @;


1.3
date	99.07.06.13.18.09;	author mudadm;	state Exp;
branches;
next	1.2;

1.2
date	99.05.24.00.34.59;	author mudadm;	state Exp;
branches;
next	1.1;

1.1
date	97.12.17.06.14.09;	author mudadm;	state Exp;
branches;
next	;


desc
@for brief
@


1.3
log
@?? by emtoe
@
text
@// move.c

#pragma optimize

#include <dbase.h>
#include <room.h>
#include <ansi.h>

static int weight = 0;
static int encumb = 0, max_encumb = 0;
static int dec_type=0;

nomask int query_encumbrance() { return encumb; }
nomask int over_encumbranced() { return encumb > max_encumb; }
int set_dec_type(int type) { dec_type = type; }

nomask int query_max_encumbrance() { return max_encumb; }
nomask void set_max_encumbrance(int e) { max_encumb = e; }
nomask void add_encumbrance(int w)
{
	encumb += w;
	if( encumb < 0 )
		log_file("move.bug", sprintf("%O encumbrance underflow: %d:%O.\n", this_object(),dec_type,environment(this_object())));
	if( encumb > max_encumb ) this_object()->over_encumbrance();
	if( environment() ) environment()->add_encumbrance(w);
}

void over_encumbrance()
{
	if( !interactive(this_object()) ) return;
	tell_object(this_object(), "你的负荷过重了！\n");
}

nomask int query_weight() { return weight; }
nomask void set_weight(int w)
{
	if( !environment() ) {
		weight = w;
		return;
	}
	if( w!=weight ) {
		environment()->set_dec_type(1); 
		environment()->add_encumbrance( w - weight );
	}
	weight = w;
}


// This is the "current" weight of an object, which is used on weight
// checking in move().
nomask int weight() { return weight + encumb; }

varargs int move(mixed dest, int silently)
{
	object ob, env ,*inv;
	mapping exits,doors;
	int i;
	string str,*dirs;

	// If we are equipped, unequip first.
	if( query("equipped") && !this_object()->unequip() )
		return notify_fail("你没有办法取下这样东西。\n");

	// Find the destination ob for moving.
	if( objectp(dest) )
		ob = dest;
	else if( stringp(dest) ) {
		call_other(dest, "???");
		ob = find_object(dest);
		if(!ob) error("move: destination unavailable.\n");
	} else
		error(sprintf("move: arg 0 object or string expected, %s passed.\n", typeof(dest)));

	// Check if the destination ob can hold this object.
	// Beforce checking it, we check if the destination is environment of
	// this_object() (or environment of its environment). If it is, then
	// this could be like get something from a bag carried by the player.
	// Since the player can carry the bag, we assume he can carry the this
	// object in the bag and encumbrance checking is unessessary.
	env = this_object();
	while(env = environment(env))
		if( env==ob ) break;

	// We only check non-characters i.e. containers here. Character can always
	// over encumbrance.
/* what so ever a player can't get more than 1000 times his encumbrance
 * by dao 980418
	if( !env
	&&	((int)ob->query_encumbrance() + weight() > (int)ob->query_max_encumbrance())
	&&	!ob->is_character() )
		return notify_fail( ob->name() + "已经放不下任何东西了。\n");
*/
	if(!env)
	{
		int encumbrance,max_encumbrance,after_encumbrance;
		encumbrance = ob->query_encumbrance();
		max_encumbrance = ob->query_max_encumbrance();
		after_encumbrance = encumbrance + weight();
// some npc may carry object before set encumbrance ;dao 980504
                  if(sizeof(all_inventory(ob))>=30 && ob->is_charactor())
                        { 
                        if (ob == this_player()) 
                          return notify_fail("你身上已经无法放下这样东西了\n");
                         else return notify_fail(ob->name()+"身上已经无法放下这样东西了.\n");
                         }   
		  
		if( (max_encumbrance>0 || !ob->is_character()) &&  after_encumbrance > max_encumbrance ) {
		   if(!ob->is_character()) 
		      return notify_fail( ob->name() + "已经放不下任何东西了。\n");
 		   else if (!userp(ob)) 
		      return notify_fail( ob->name() + "看起来拿不动这个东西\n");
                      else if( after_encumbrance > max_encumbrance * 5) {
		             if(ob==this_player()) 
		               return notify_fail( this_object()->name() + "对你而言太重了。\n");
			    else 
			      return notify_fail( "这"+this_object()->name() + "对" + ob->name() + "而言太重了。\n");
		       }
		}
	}
	// Move the object and update encumbrance
	if( environment() ) {
		environment()->set_dec_type(2);
		environment()->add_encumbrance( - weight());
	}
	move_object(ob);
	ob->add_encumbrance(weight());

	// If we are players, try look where we are.
	if( interactive(this_object())		// are we linkdead?
	&&	living(this_object())			// are we still concious?
	&&	!silently ) {
		if( query("env/brief") )
		{
                        str = environment()->query("short") + "\n";
			if( mapp(exits = environment()->query("exits")) )
			dirs = keys(exits);
	
			if( mapp(doors = environment()->query_doors()) ) 
			{
			//filter_array( keys(exits),
			//	(: ((int)->query_door($1, "status") & DOOR_CLOSED)==0 :) );
			for(i=0; i<sizeof(dirs); i++)
				if( mapp(doors[dirs[i]])
				&&	(doors[dirs[i]]["status"] & DOOR_CLOSED) )
					dirs[i] = 0;
			dirs -= ({ 0 });
			}

			if( sizeof(dirs)==0 )
				str += "    这里没有任何明显的出路。\n";
			else if( sizeof(dirs)==1 )
				str += "    这里唯一的出口是 " BOLD + dirs[0] + NOR "。\n";
			else
				str += sprintf("    这里明显的出口是 " BOLD "%s" NOR " 和 "
						 BOLD "%s" NOR "。\n",
						implode(dirs[0..sizeof(dirs)-2], "、"),
						dirs[sizeof(dirs)-1]);
	
                        inv = all_inventory(environment());
                        for(i=0; i<sizeof(inv); i++) 
			{
                                if( inv[i]==this_object() ) continue;
		        	if( inv[i]->query("no_shown")) continue;
                                if( !this_object()->visible(inv[i]) ) continue;
                                str += "  " + inv[i]->short() + "\n";
                        }

			message("vision", str, this_object());
		}
		else
			command("look");
	}

	return 1;
}

void remove(string euid)
{
	object default_ob;

	if( !previous_object()
	||	base_name(previous_object()) != SIMUL_EFUN_OB )
		error("move: remove() can only be called by destruct() simul efun.\n");

	if( userp(this_object()) && euid!=ROOT_UID ) {
		log_file("destruct", sprintf("%s attempt to destruct user object %s (%s)\n",
			euid, this_object()->query("id"), ctime(time())));
		error("你(" + euid + ")不能摧毁其他的使用者。\n");
	} else if( this_object()->query("equipped")) {
		if(	!this_object()->unequip() )
			log_file("destruct", sprintf("Failed to unequip %s when destructed.\n",file_name(this_object())));
	}

	// We only care about our own weight here, since remove() is called once
	// on each destruct(), so our inventory (encumbrance) will be counted as
	// well.
	if( environment() )	{
		environment()->set_dec_type(3);
		environment()->add_encumbrance( - weight );
	}
	if( default_ob = this_object()->query_default_object() )
		default_ob->add("no_clean_up", -1);
}

int move_or_destruct( object dest )
{
	if( userp(this_object()) ) {
		tell_object(this_object(), "一阵时空的扭曲将你传送到另一个地方....\n");
		move(VOID_OB);
	}
}
@


1.2
log
@restrict 30 obj
,
@
text
@d100 7
d112 5
a116 5
		   else if( after_encumbrance > max_encumbrance * 5) {
		          if(ob==this_player()) 
		             return notify_fail( this_object()->name() + "对你而言太重了。\n");
			  else 
			     return notify_fail( "这"+this_object()->name() + "对" + ob->name() + "而言太重了。\n");
@


1.1
log
@Initial revision
@
text
@d6 2
d11 1
d15 1
d23 1
a23 1
		log_file("move.bug", sprintf("%O encumbrance underflow.\n", this_object()));
d41 4
a44 1
	if( w!=weight ) environment()->add_encumbrance( w - weight );
d55 4
a58 2
	object ob, env;
	int w;
d86 2
d92 21
a112 1

d114 4
a117 1
	if( environment() ) environment()->add_encumbrance( - weight());
d126 37
a162 1
			message("vision", environment()->query("short") + "\n", this_object());
d190 4
a193 1
	if( environment() )	environment()->add_encumbrance( - weight );
@
